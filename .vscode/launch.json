{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "WolfTrapOysters",
            "type": "php",
            "request": "launch",
            "program": "${workspaceFolder}/index.php",
            "runtimeExecutable": "php",
            "runtimeArgs": [
                "-S",
                "localhost:8000",
                "-t",
                "${workspaceFolder}"
            ],
            "port": 8000,
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false
        }
    ]
}
